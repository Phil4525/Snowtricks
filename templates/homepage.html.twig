{% extends 'base.html.twig' %}

{% block title %}
Home
{% endblock %}

{% block body %}
<main>
  <div class="px-4 py-5 mb-5 text-center hero">
    {# <h1 class="display-5 fw-bold">Centered hero</h1>
    <div class="col-lg-6 mx-auto">
      <p class="lead mb-4">Quickly design and customize responsive mobile-first sites with Bootstrap, the world’s most
        popular front-end open source toolkit, featuring Sass variables and mixins, responsive grid system, extensive
        prebuilt components, and powerful JavaScript plugins.</p> #}
      <div class="position-absolute bottom-0 start-50 pb-4">
        <a href="#tricks"><i class="fa-solid fa-circle-chevron-down fs-1 herobtn"></i></a>
      </div>
    </div>
  </div>
  <div>
    <div class="container" id="tricks">
      <div class="mb-4">
        <div class="row row-cols-1 row-cols-sm-2 row-cols-md-3 row-cols-lg-4 row-cols-xl-5 g-4 mb-4">
          {% for trick in tricks %}
          <div class="col">
            <div class="card">
              <img class="img-fluid" src="https://dummyimage.com/388x258/dee1e6/697379.jpg" alt="..." />
              <div class="card-body d-flex justify-content-between p-2">
                <a class="text-decoration-none text-dark" href="{{ path('app_trick', {id:trick.id}) }}">
                  <h5 class="card-title fs-6 text-uppercase">{{ trick.name }}</h5>
                </a>
                <div>
                  <a href="" class="text-decoration-none fa-sharp fa-solid fa-pen"></a>
                  <a href="" class="text-decoration-none fa-solid fa-trash-can ms-2"></a>
                </div>
              </div>
            </div>
          </div>
          {% endfor %}
        </div>
        <div class="text-center">
          <a href=""><i class="fa-solid fa-circle-plus fs-1"></i></a>
        </div>
      </div>
    </div>
    <div class="sticky-bottom float-end d-none d-xl-block mt-3 pe-3 pb-3">
      <a href="#tricks"><i class="fa-solid fa-circle-chevron-up fs-1"></i></a>
    </div>
  </div>
</main>
{% endblock %}